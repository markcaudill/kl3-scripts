#!/bin/sh
# lock the session and save slock's mugshots

xset dpms force off
pkill -SIGKILL xbattmon\|setdwmstat

slock ${@}

xbattmon &
setdwmstat &

if [ -f ~/slock.jpg ]; then
	filepath=~/slock-pictures/slock-$(date +%F-%T).jpg
	mv -f ~/slock.jpg ${filepath} && sxiv -f -q ${filepath}
fi
