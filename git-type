#!/bin/sh
# type(1) for git(1) aliases

alias=$(sed -n "/^\[alias\]\$/,\${
			/^\t*${1}\t*= /p
		}" "${HOME}"/.gitconfig)

# Print definition if found otherwise raise error
printf '%s\n' "${alias:?${1} not found}"
