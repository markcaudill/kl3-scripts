#!/bin/sh
# set dwm's status

while (true)
do
        xsetroot -name "$(printf '%s  %s  E: %s  W: %s  %d°C  %d%%  %s' \
                "$(getsong)" \
                "$(getvpn)" \
                "$(geteth)" \
                "$(getwifi)" \
                "$(gettemp)" \
                "$(getvol)" \
                "$(date '+%a %d.%m.%y %H:%M')" \
        )"
	sleep 30
done
